// Use an array instead of a real database for simplicity in this example
// Normally, this would be pre-populated, and the filter generated from it

// todo: replace this with calls to a GraphQL API
// on a node server that talks to a MongoDB (or other) database

const db = [];
module.exports = db